// Code generated by SQLBoiler 4.14.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// UserMonsterKillHistory is an object representing the database table.
type UserMonsterKillHistory struct {
	ID int64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	// ユーザーID
	UserID int64 `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	// モンスターID
	MonsterID int64 `boil:"monster_id" json:"monster_id" toml:"monster_id" yaml:"monster_id"`
	// 討伐日
	KilledAt  time.Time `boil:"killed_at" json:"killed_at" toml:"killed_at" yaml:"killed_at"`
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *userMonsterKillHistoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userMonsterKillHistoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserMonsterKillHistoryColumns = struct {
	ID        string
	UserID    string
	MonsterID string
	KilledAt  string
	UpdatedAt string
	CreatedAt string
}{
	ID:        "id",
	UserID:    "user_id",
	MonsterID: "monster_id",
	KilledAt:  "killed_at",
	UpdatedAt: "updated_at",
	CreatedAt: "created_at",
}

var UserMonsterKillHistoryTableColumns = struct {
	ID        string
	UserID    string
	MonsterID string
	KilledAt  string
	UpdatedAt string
	CreatedAt string
}{
	ID:        "user_monster_kill_histories.id",
	UserID:    "user_monster_kill_histories.user_id",
	MonsterID: "user_monster_kill_histories.monster_id",
	KilledAt:  "user_monster_kill_histories.killed_at",
	UpdatedAt: "user_monster_kill_histories.updated_at",
	CreatedAt: "user_monster_kill_histories.created_at",
}

// Generated where

var UserMonsterKillHistoryWhere = struct {
	ID        whereHelperint64
	UserID    whereHelperint64
	MonsterID whereHelperint64
	KilledAt  whereHelpertime_Time
	UpdatedAt whereHelpertime_Time
	CreatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "\"user_monster_kill_histories\".\"id\""},
	UserID:    whereHelperint64{field: "\"user_monster_kill_histories\".\"user_id\""},
	MonsterID: whereHelperint64{field: "\"user_monster_kill_histories\".\"monster_id\""},
	KilledAt:  whereHelpertime_Time{field: "\"user_monster_kill_histories\".\"killed_at\""},
	UpdatedAt: whereHelpertime_Time{field: "\"user_monster_kill_histories\".\"updated_at\""},
	CreatedAt: whereHelpertime_Time{field: "\"user_monster_kill_histories\".\"created_at\""},
}

// UserMonsterKillHistoryRels is where relationship names are stored.
var UserMonsterKillHistoryRels = struct {
	Monster string
	User    string
}{
	Monster: "Monster",
	User:    "User",
}

// userMonsterKillHistoryR is where relationships are stored.
type userMonsterKillHistoryR struct {
	Monster *Monster `boil:"Monster" json:"Monster" toml:"Monster" yaml:"Monster"`
	User    *User    `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*userMonsterKillHistoryR) NewStruct() *userMonsterKillHistoryR {
	return &userMonsterKillHistoryR{}
}

func (r *userMonsterKillHistoryR) GetMonster() *Monster {
	if r == nil {
		return nil
	}
	return r.Monster
}

func (r *userMonsterKillHistoryR) GetUser() *User {
	if r == nil {
		return nil
	}
	return r.User
}

// userMonsterKillHistoryL is where Load methods for each relationship are stored.
type userMonsterKillHistoryL struct{}

var (
	userMonsterKillHistoryAllColumns            = []string{"id", "user_id", "monster_id", "killed_at", "updated_at", "created_at"}
	userMonsterKillHistoryColumnsWithoutDefault = []string{"user_id", "monster_id"}
	userMonsterKillHistoryColumnsWithDefault    = []string{"id", "killed_at", "updated_at", "created_at"}
	userMonsterKillHistoryPrimaryKeyColumns     = []string{"id"}
	userMonsterKillHistoryGeneratedColumns      = []string{}
)

type (
	// UserMonsterKillHistorySlice is an alias for a slice of pointers to UserMonsterKillHistory.
	// This should almost always be used instead of []UserMonsterKillHistory.
	UserMonsterKillHistorySlice []*UserMonsterKillHistory
	// UserMonsterKillHistoryHook is the signature for custom UserMonsterKillHistory hook methods
	UserMonsterKillHistoryHook func(context.Context, boil.ContextExecutor, *UserMonsterKillHistory) error

	userMonsterKillHistoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userMonsterKillHistoryType                 = reflect.TypeOf(&UserMonsterKillHistory{})
	userMonsterKillHistoryMapping              = queries.MakeStructMapping(userMonsterKillHistoryType)
	userMonsterKillHistoryPrimaryKeyMapping, _ = queries.BindMapping(userMonsterKillHistoryType, userMonsterKillHistoryMapping, userMonsterKillHistoryPrimaryKeyColumns)
	userMonsterKillHistoryInsertCacheMut       sync.RWMutex
	userMonsterKillHistoryInsertCache          = make(map[string]insertCache)
	userMonsterKillHistoryUpdateCacheMut       sync.RWMutex
	userMonsterKillHistoryUpdateCache          = make(map[string]updateCache)
	userMonsterKillHistoryUpsertCacheMut       sync.RWMutex
	userMonsterKillHistoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var userMonsterKillHistoryAfterSelectHooks []UserMonsterKillHistoryHook

var userMonsterKillHistoryBeforeInsertHooks []UserMonsterKillHistoryHook
var userMonsterKillHistoryAfterInsertHooks []UserMonsterKillHistoryHook

var userMonsterKillHistoryBeforeUpdateHooks []UserMonsterKillHistoryHook
var userMonsterKillHistoryAfterUpdateHooks []UserMonsterKillHistoryHook

var userMonsterKillHistoryBeforeDeleteHooks []UserMonsterKillHistoryHook
var userMonsterKillHistoryAfterDeleteHooks []UserMonsterKillHistoryHook

var userMonsterKillHistoryBeforeUpsertHooks []UserMonsterKillHistoryHook
var userMonsterKillHistoryAfterUpsertHooks []UserMonsterKillHistoryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *UserMonsterKillHistory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *UserMonsterKillHistory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *UserMonsterKillHistory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *UserMonsterKillHistory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *UserMonsterKillHistory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *UserMonsterKillHistory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *UserMonsterKillHistory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *UserMonsterKillHistory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *UserMonsterKillHistory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range userMonsterKillHistoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddUserMonsterKillHistoryHook registers your hook function for all future operations.
func AddUserMonsterKillHistoryHook(hookPoint boil.HookPoint, userMonsterKillHistoryHook UserMonsterKillHistoryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		userMonsterKillHistoryAfterSelectHooks = append(userMonsterKillHistoryAfterSelectHooks, userMonsterKillHistoryHook)
	case boil.BeforeInsertHook:
		userMonsterKillHistoryBeforeInsertHooks = append(userMonsterKillHistoryBeforeInsertHooks, userMonsterKillHistoryHook)
	case boil.AfterInsertHook:
		userMonsterKillHistoryAfterInsertHooks = append(userMonsterKillHistoryAfterInsertHooks, userMonsterKillHistoryHook)
	case boil.BeforeUpdateHook:
		userMonsterKillHistoryBeforeUpdateHooks = append(userMonsterKillHistoryBeforeUpdateHooks, userMonsterKillHistoryHook)
	case boil.AfterUpdateHook:
		userMonsterKillHistoryAfterUpdateHooks = append(userMonsterKillHistoryAfterUpdateHooks, userMonsterKillHistoryHook)
	case boil.BeforeDeleteHook:
		userMonsterKillHistoryBeforeDeleteHooks = append(userMonsterKillHistoryBeforeDeleteHooks, userMonsterKillHistoryHook)
	case boil.AfterDeleteHook:
		userMonsterKillHistoryAfterDeleteHooks = append(userMonsterKillHistoryAfterDeleteHooks, userMonsterKillHistoryHook)
	case boil.BeforeUpsertHook:
		userMonsterKillHistoryBeforeUpsertHooks = append(userMonsterKillHistoryBeforeUpsertHooks, userMonsterKillHistoryHook)
	case boil.AfterUpsertHook:
		userMonsterKillHistoryAfterUpsertHooks = append(userMonsterKillHistoryAfterUpsertHooks, userMonsterKillHistoryHook)
	}
}

// One returns a single userMonsterKillHistory record from the query.
func (q userMonsterKillHistoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*UserMonsterKillHistory, error) {
	o := &UserMonsterKillHistory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for user_monster_kill_histories")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all UserMonsterKillHistory records from the query.
func (q userMonsterKillHistoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (UserMonsterKillHistorySlice, error) {
	var o []*UserMonsterKillHistory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to UserMonsterKillHistory slice")
	}

	if len(userMonsterKillHistoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all UserMonsterKillHistory records in the query.
func (q userMonsterKillHistoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count user_monster_kill_histories rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q userMonsterKillHistoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if user_monster_kill_histories exists")
	}

	return count > 0, nil
}

// Monster pointed to by the foreign key.
func (o *UserMonsterKillHistory) Monster(mods ...qm.QueryMod) monsterQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.MonsterID),
	}

	queryMods = append(queryMods, mods...)

	return Monsters(queryMods...)
}

// User pointed to by the foreign key.
func (o *UserMonsterKillHistory) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadMonster allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userMonsterKillHistoryL) LoadMonster(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserMonsterKillHistory interface{}, mods queries.Applicator) error {
	var slice []*UserMonsterKillHistory
	var object *UserMonsterKillHistory

	if singular {
		var ok bool
		object, ok = maybeUserMonsterKillHistory.(*UserMonsterKillHistory)
		if !ok {
			object = new(UserMonsterKillHistory)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserMonsterKillHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserMonsterKillHistory))
			}
		}
	} else {
		s, ok := maybeUserMonsterKillHistory.(*[]*UserMonsterKillHistory)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserMonsterKillHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserMonsterKillHistory))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &userMonsterKillHistoryR{}
		}
		args = append(args, object.MonsterID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userMonsterKillHistoryR{}
			}

			for _, a := range args {
				if a == obj.MonsterID {
					continue Outer
				}
			}

			args = append(args, obj.MonsterID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`monsters`),
		qm.WhereIn(`monsters.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Monster")
	}

	var resultSlice []*Monster
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Monster")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for monsters")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for monsters")
	}

	if len(monsterAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Monster = foreign
		if foreign.R == nil {
			foreign.R = &monsterR{}
		}
		foreign.R.UserMonsterKillHistories = append(foreign.R.UserMonsterKillHistories, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MonsterID == foreign.ID {
				local.R.Monster = foreign
				if foreign.R == nil {
					foreign.R = &monsterR{}
				}
				foreign.R.UserMonsterKillHistories = append(foreign.R.UserMonsterKillHistories, local)
				break
			}
		}
	}

	return nil
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userMonsterKillHistoryL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUserMonsterKillHistory interface{}, mods queries.Applicator) error {
	var slice []*UserMonsterKillHistory
	var object *UserMonsterKillHistory

	if singular {
		var ok bool
		object, ok = maybeUserMonsterKillHistory.(*UserMonsterKillHistory)
		if !ok {
			object = new(UserMonsterKillHistory)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUserMonsterKillHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUserMonsterKillHistory))
			}
		}
	} else {
		s, ok := maybeUserMonsterKillHistory.(*[]*UserMonsterKillHistory)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUserMonsterKillHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUserMonsterKillHistory))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &userMonsterKillHistoryR{}
		}
		args = append(args, object.UserID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userMonsterKillHistoryR{}
			}

			for _, a := range args {
				if a == obj.UserID {
					continue Outer
				}
			}

			args = append(args, obj.UserID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.UserMonsterKillHistories = append(foreign.R.UserMonsterKillHistories, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.UserMonsterKillHistories = append(foreign.R.UserMonsterKillHistories, local)
				break
			}
		}
	}

	return nil
}

// SetMonster of the userMonsterKillHistory to the related item.
// Sets o.R.Monster to related.
// Adds o to related.R.UserMonsterKillHistories.
func (o *UserMonsterKillHistory) SetMonster(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Monster) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_monster_kill_histories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"monster_id"}),
		strmangle.WhereClause("\"", "\"", 2, userMonsterKillHistoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MonsterID = related.ID
	if o.R == nil {
		o.R = &userMonsterKillHistoryR{
			Monster: related,
		}
	} else {
		o.R.Monster = related
	}

	if related.R == nil {
		related.R = &monsterR{
			UserMonsterKillHistories: UserMonsterKillHistorySlice{o},
		}
	} else {
		related.R.UserMonsterKillHistories = append(related.R.UserMonsterKillHistories, o)
	}

	return nil
}

// SetUser of the userMonsterKillHistory to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserMonsterKillHistories.
func (o *UserMonsterKillHistory) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"user_monster_kill_histories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, userMonsterKillHistoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &userMonsterKillHistoryR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			UserMonsterKillHistories: UserMonsterKillHistorySlice{o},
		}
	} else {
		related.R.UserMonsterKillHistories = append(related.R.UserMonsterKillHistories, o)
	}

	return nil
}

// UserMonsterKillHistories retrieves all the records using an executor.
func UserMonsterKillHistories(mods ...qm.QueryMod) userMonsterKillHistoryQuery {
	mods = append(mods, qm.From("\"user_monster_kill_histories\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"user_monster_kill_histories\".*"})
	}

	return userMonsterKillHistoryQuery{q}
}

// FindUserMonsterKillHistory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUserMonsterKillHistory(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*UserMonsterKillHistory, error) {
	userMonsterKillHistoryObj := &UserMonsterKillHistory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"user_monster_kill_histories\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, userMonsterKillHistoryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from user_monster_kill_histories")
	}

	if err = userMonsterKillHistoryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return userMonsterKillHistoryObj, err
	}

	return userMonsterKillHistoryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *UserMonsterKillHistory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_monster_kill_histories provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userMonsterKillHistoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userMonsterKillHistoryInsertCacheMut.RLock()
	cache, cached := userMonsterKillHistoryInsertCache[key]
	userMonsterKillHistoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userMonsterKillHistoryAllColumns,
			userMonsterKillHistoryColumnsWithDefault,
			userMonsterKillHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(userMonsterKillHistoryType, userMonsterKillHistoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userMonsterKillHistoryType, userMonsterKillHistoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"user_monster_kill_histories\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"user_monster_kill_histories\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into user_monster_kill_histories")
	}

	if !cached {
		userMonsterKillHistoryInsertCacheMut.Lock()
		userMonsterKillHistoryInsertCache[key] = cache
		userMonsterKillHistoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the UserMonsterKillHistory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *UserMonsterKillHistory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	userMonsterKillHistoryUpdateCacheMut.RLock()
	cache, cached := userMonsterKillHistoryUpdateCache[key]
	userMonsterKillHistoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userMonsterKillHistoryAllColumns,
			userMonsterKillHistoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update user_monster_kill_histories, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"user_monster_kill_histories\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userMonsterKillHistoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userMonsterKillHistoryType, userMonsterKillHistoryMapping, append(wl, userMonsterKillHistoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update user_monster_kill_histories row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for user_monster_kill_histories")
	}

	if !cached {
		userMonsterKillHistoryUpdateCacheMut.Lock()
		userMonsterKillHistoryUpdateCache[key] = cache
		userMonsterKillHistoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q userMonsterKillHistoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for user_monster_kill_histories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for user_monster_kill_histories")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserMonsterKillHistorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userMonsterKillHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"user_monster_kill_histories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userMonsterKillHistoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in userMonsterKillHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all userMonsterKillHistory")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *UserMonsterKillHistory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no user_monster_kill_histories provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(userMonsterKillHistoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userMonsterKillHistoryUpsertCacheMut.RLock()
	cache, cached := userMonsterKillHistoryUpsertCache[key]
	userMonsterKillHistoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			userMonsterKillHistoryAllColumns,
			userMonsterKillHistoryColumnsWithDefault,
			userMonsterKillHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			userMonsterKillHistoryAllColumns,
			userMonsterKillHistoryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert user_monster_kill_histories, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(userMonsterKillHistoryPrimaryKeyColumns))
			copy(conflict, userMonsterKillHistoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"user_monster_kill_histories\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(userMonsterKillHistoryType, userMonsterKillHistoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userMonsterKillHistoryType, userMonsterKillHistoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert user_monster_kill_histories")
	}

	if !cached {
		userMonsterKillHistoryUpsertCacheMut.Lock()
		userMonsterKillHistoryUpsertCache[key] = cache
		userMonsterKillHistoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single UserMonsterKillHistory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *UserMonsterKillHistory) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no UserMonsterKillHistory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userMonsterKillHistoryPrimaryKeyMapping)
	sql := "DELETE FROM \"user_monster_kill_histories\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from user_monster_kill_histories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for user_monster_kill_histories")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userMonsterKillHistoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no userMonsterKillHistoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from user_monster_kill_histories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_monster_kill_histories")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserMonsterKillHistorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(userMonsterKillHistoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userMonsterKillHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"user_monster_kill_histories\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userMonsterKillHistoryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from userMonsterKillHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for user_monster_kill_histories")
	}

	if len(userMonsterKillHistoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *UserMonsterKillHistory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUserMonsterKillHistory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserMonsterKillHistorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserMonsterKillHistorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userMonsterKillHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"user_monster_kill_histories\".* FROM \"user_monster_kill_histories\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userMonsterKillHistoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UserMonsterKillHistorySlice")
	}

	*o = slice

	return nil
}

// UserMonsterKillHistoryExists checks if the UserMonsterKillHistory row exists.
func UserMonsterKillHistoryExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"user_monster_kill_histories\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if user_monster_kill_histories exists")
	}

	return exists, nil
}

// Exists checks if the UserMonsterKillHistory row exists.
func (o *UserMonsterKillHistory) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UserMonsterKillHistoryExists(ctx, exec, o.ID)
}
