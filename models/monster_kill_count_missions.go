// Code generated by SQLBoiler 4.14.2 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MonsterKillCountMission is an object representing the database table.
type MonsterKillCountMission struct {
	ID int64 `boil:"id" json:"id" toml:"id" yaml:"id"`
	// ミッションID
	MissionID int64 `boil:"mission_id" json:"mission_id" toml:"mission_id" yaml:"mission_id"`
	// 討伐必要モンスター数
	KillCount int64     `boil:"kill_count" json:"kill_count" toml:"kill_count" yaml:"kill_count"`
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *monsterKillCountMissionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L monsterKillCountMissionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MonsterKillCountMissionColumns = struct {
	ID        string
	MissionID string
	KillCount string
	UpdatedAt string
	CreatedAt string
}{
	ID:        "id",
	MissionID: "mission_id",
	KillCount: "kill_count",
	UpdatedAt: "updated_at",
	CreatedAt: "created_at",
}

var MonsterKillCountMissionTableColumns = struct {
	ID        string
	MissionID string
	KillCount string
	UpdatedAt string
	CreatedAt string
}{
	ID:        "monster_kill_count_missions.id",
	MissionID: "monster_kill_count_missions.mission_id",
	KillCount: "monster_kill_count_missions.kill_count",
	UpdatedAt: "monster_kill_count_missions.updated_at",
	CreatedAt: "monster_kill_count_missions.created_at",
}

// Generated where

var MonsterKillCountMissionWhere = struct {
	ID        whereHelperint64
	MissionID whereHelperint64
	KillCount whereHelperint64
	UpdatedAt whereHelpertime_Time
	CreatedAt whereHelpertime_Time
}{
	ID:        whereHelperint64{field: "\"monster_kill_count_missions\".\"id\""},
	MissionID: whereHelperint64{field: "\"monster_kill_count_missions\".\"mission_id\""},
	KillCount: whereHelperint64{field: "\"monster_kill_count_missions\".\"kill_count\""},
	UpdatedAt: whereHelpertime_Time{field: "\"monster_kill_count_missions\".\"updated_at\""},
	CreatedAt: whereHelpertime_Time{field: "\"monster_kill_count_missions\".\"created_at\""},
}

// MonsterKillCountMissionRels is where relationship names are stored.
var MonsterKillCountMissionRels = struct {
	Mission string
}{
	Mission: "Mission",
}

// monsterKillCountMissionR is where relationships are stored.
type monsterKillCountMissionR struct {
	Mission *Mission `boil:"Mission" json:"Mission" toml:"Mission" yaml:"Mission"`
}

// NewStruct creates a new relationship struct
func (*monsterKillCountMissionR) NewStruct() *monsterKillCountMissionR {
	return &monsterKillCountMissionR{}
}

func (r *monsterKillCountMissionR) GetMission() *Mission {
	if r == nil {
		return nil
	}
	return r.Mission
}

// monsterKillCountMissionL is where Load methods for each relationship are stored.
type monsterKillCountMissionL struct{}

var (
	monsterKillCountMissionAllColumns            = []string{"id", "mission_id", "kill_count", "updated_at", "created_at"}
	monsterKillCountMissionColumnsWithoutDefault = []string{"mission_id", "kill_count"}
	monsterKillCountMissionColumnsWithDefault    = []string{"id", "updated_at", "created_at"}
	monsterKillCountMissionPrimaryKeyColumns     = []string{"id"}
	monsterKillCountMissionGeneratedColumns      = []string{}
)

type (
	// MonsterKillCountMissionSlice is an alias for a slice of pointers to MonsterKillCountMission.
	// This should almost always be used instead of []MonsterKillCountMission.
	MonsterKillCountMissionSlice []*MonsterKillCountMission
	// MonsterKillCountMissionHook is the signature for custom MonsterKillCountMission hook methods
	MonsterKillCountMissionHook func(context.Context, boil.ContextExecutor, *MonsterKillCountMission) error

	monsterKillCountMissionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	monsterKillCountMissionType                 = reflect.TypeOf(&MonsterKillCountMission{})
	monsterKillCountMissionMapping              = queries.MakeStructMapping(monsterKillCountMissionType)
	monsterKillCountMissionPrimaryKeyMapping, _ = queries.BindMapping(monsterKillCountMissionType, monsterKillCountMissionMapping, monsterKillCountMissionPrimaryKeyColumns)
	monsterKillCountMissionInsertCacheMut       sync.RWMutex
	monsterKillCountMissionInsertCache          = make(map[string]insertCache)
	monsterKillCountMissionUpdateCacheMut       sync.RWMutex
	monsterKillCountMissionUpdateCache          = make(map[string]updateCache)
	monsterKillCountMissionUpsertCacheMut       sync.RWMutex
	monsterKillCountMissionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var monsterKillCountMissionAfterSelectHooks []MonsterKillCountMissionHook

var monsterKillCountMissionBeforeInsertHooks []MonsterKillCountMissionHook
var monsterKillCountMissionAfterInsertHooks []MonsterKillCountMissionHook

var monsterKillCountMissionBeforeUpdateHooks []MonsterKillCountMissionHook
var monsterKillCountMissionAfterUpdateHooks []MonsterKillCountMissionHook

var monsterKillCountMissionBeforeDeleteHooks []MonsterKillCountMissionHook
var monsterKillCountMissionAfterDeleteHooks []MonsterKillCountMissionHook

var monsterKillCountMissionBeforeUpsertHooks []MonsterKillCountMissionHook
var monsterKillCountMissionAfterUpsertHooks []MonsterKillCountMissionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MonsterKillCountMission) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MonsterKillCountMission) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MonsterKillCountMission) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MonsterKillCountMission) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MonsterKillCountMission) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MonsterKillCountMission) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MonsterKillCountMission) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MonsterKillCountMission) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MonsterKillCountMission) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterKillCountMissionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMonsterKillCountMissionHook registers your hook function for all future operations.
func AddMonsterKillCountMissionHook(hookPoint boil.HookPoint, monsterKillCountMissionHook MonsterKillCountMissionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		monsterKillCountMissionAfterSelectHooks = append(monsterKillCountMissionAfterSelectHooks, monsterKillCountMissionHook)
	case boil.BeforeInsertHook:
		monsterKillCountMissionBeforeInsertHooks = append(monsterKillCountMissionBeforeInsertHooks, monsterKillCountMissionHook)
	case boil.AfterInsertHook:
		monsterKillCountMissionAfterInsertHooks = append(monsterKillCountMissionAfterInsertHooks, monsterKillCountMissionHook)
	case boil.BeforeUpdateHook:
		monsterKillCountMissionBeforeUpdateHooks = append(monsterKillCountMissionBeforeUpdateHooks, monsterKillCountMissionHook)
	case boil.AfterUpdateHook:
		monsterKillCountMissionAfterUpdateHooks = append(monsterKillCountMissionAfterUpdateHooks, monsterKillCountMissionHook)
	case boil.BeforeDeleteHook:
		monsterKillCountMissionBeforeDeleteHooks = append(monsterKillCountMissionBeforeDeleteHooks, monsterKillCountMissionHook)
	case boil.AfterDeleteHook:
		monsterKillCountMissionAfterDeleteHooks = append(monsterKillCountMissionAfterDeleteHooks, monsterKillCountMissionHook)
	case boil.BeforeUpsertHook:
		monsterKillCountMissionBeforeUpsertHooks = append(monsterKillCountMissionBeforeUpsertHooks, monsterKillCountMissionHook)
	case boil.AfterUpsertHook:
		monsterKillCountMissionAfterUpsertHooks = append(monsterKillCountMissionAfterUpsertHooks, monsterKillCountMissionHook)
	}
}

// One returns a single monsterKillCountMission record from the query.
func (q monsterKillCountMissionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MonsterKillCountMission, error) {
	o := &MonsterKillCountMission{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for monster_kill_count_missions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MonsterKillCountMission records from the query.
func (q monsterKillCountMissionQuery) All(ctx context.Context, exec boil.ContextExecutor) (MonsterKillCountMissionSlice, error) {
	var o []*MonsterKillCountMission

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to MonsterKillCountMission slice")
	}

	if len(monsterKillCountMissionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MonsterKillCountMission records in the query.
func (q monsterKillCountMissionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count monster_kill_count_missions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q monsterKillCountMissionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if monster_kill_count_missions exists")
	}

	return count > 0, nil
}

// Mission pointed to by the foreign key.
func (o *MonsterKillCountMission) Mission(mods ...qm.QueryMod) missionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.MissionID),
	}

	queryMods = append(queryMods, mods...)

	return Missions(queryMods...)
}

// LoadMission allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (monsterKillCountMissionL) LoadMission(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMonsterKillCountMission interface{}, mods queries.Applicator) error {
	var slice []*MonsterKillCountMission
	var object *MonsterKillCountMission

	if singular {
		var ok bool
		object, ok = maybeMonsterKillCountMission.(*MonsterKillCountMission)
		if !ok {
			object = new(MonsterKillCountMission)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeMonsterKillCountMission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeMonsterKillCountMission))
			}
		}
	} else {
		s, ok := maybeMonsterKillCountMission.(*[]*MonsterKillCountMission)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeMonsterKillCountMission)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeMonsterKillCountMission))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &monsterKillCountMissionR{}
		}
		args = append(args, object.MissionID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &monsterKillCountMissionR{}
			}

			for _, a := range args {
				if a == obj.MissionID {
					continue Outer
				}
			}

			args = append(args, obj.MissionID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`missions`),
		qm.WhereIn(`missions.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Mission")
	}

	var resultSlice []*Mission
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Mission")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for missions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for missions")
	}

	if len(missionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Mission = foreign
		if foreign.R == nil {
			foreign.R = &missionR{}
		}
		foreign.R.MonsterKillCountMission = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MissionID == foreign.ID {
				local.R.Mission = foreign
				if foreign.R == nil {
					foreign.R = &missionR{}
				}
				foreign.R.MonsterKillCountMission = local
				break
			}
		}
	}

	return nil
}

// SetMission of the monsterKillCountMission to the related item.
// Sets o.R.Mission to related.
// Adds o to related.R.MonsterKillCountMission.
func (o *MonsterKillCountMission) SetMission(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Mission) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"monster_kill_count_missions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"mission_id"}),
		strmangle.WhereClause("\"", "\"", 2, monsterKillCountMissionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MissionID = related.ID
	if o.R == nil {
		o.R = &monsterKillCountMissionR{
			Mission: related,
		}
	} else {
		o.R.Mission = related
	}

	if related.R == nil {
		related.R = &missionR{
			MonsterKillCountMission: o,
		}
	} else {
		related.R.MonsterKillCountMission = o
	}

	return nil
}

// MonsterKillCountMissions retrieves all the records using an executor.
func MonsterKillCountMissions(mods ...qm.QueryMod) monsterKillCountMissionQuery {
	mods = append(mods, qm.From("\"monster_kill_count_missions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"monster_kill_count_missions\".*"})
	}

	return monsterKillCountMissionQuery{q}
}

// FindMonsterKillCountMission retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMonsterKillCountMission(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*MonsterKillCountMission, error) {
	monsterKillCountMissionObj := &MonsterKillCountMission{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"monster_kill_count_missions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, monsterKillCountMissionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from monster_kill_count_missions")
	}

	if err = monsterKillCountMissionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return monsterKillCountMissionObj, err
	}

	return monsterKillCountMissionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MonsterKillCountMission) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no monster_kill_count_missions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(monsterKillCountMissionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	monsterKillCountMissionInsertCacheMut.RLock()
	cache, cached := monsterKillCountMissionInsertCache[key]
	monsterKillCountMissionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			monsterKillCountMissionAllColumns,
			monsterKillCountMissionColumnsWithDefault,
			monsterKillCountMissionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(monsterKillCountMissionType, monsterKillCountMissionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(monsterKillCountMissionType, monsterKillCountMissionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"monster_kill_count_missions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"monster_kill_count_missions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into monster_kill_count_missions")
	}

	if !cached {
		monsterKillCountMissionInsertCacheMut.Lock()
		monsterKillCountMissionInsertCache[key] = cache
		monsterKillCountMissionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the MonsterKillCountMission.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MonsterKillCountMission) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	monsterKillCountMissionUpdateCacheMut.RLock()
	cache, cached := monsterKillCountMissionUpdateCache[key]
	monsterKillCountMissionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			monsterKillCountMissionAllColumns,
			monsterKillCountMissionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update monster_kill_count_missions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"monster_kill_count_missions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, monsterKillCountMissionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(monsterKillCountMissionType, monsterKillCountMissionMapping, append(wl, monsterKillCountMissionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update monster_kill_count_missions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for monster_kill_count_missions")
	}

	if !cached {
		monsterKillCountMissionUpdateCacheMut.Lock()
		monsterKillCountMissionUpdateCache[key] = cache
		monsterKillCountMissionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q monsterKillCountMissionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for monster_kill_count_missions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for monster_kill_count_missions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MonsterKillCountMissionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), monsterKillCountMissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"monster_kill_count_missions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, monsterKillCountMissionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in monsterKillCountMission slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all monsterKillCountMission")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MonsterKillCountMission) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no monster_kill_count_missions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(monsterKillCountMissionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	monsterKillCountMissionUpsertCacheMut.RLock()
	cache, cached := monsterKillCountMissionUpsertCache[key]
	monsterKillCountMissionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			monsterKillCountMissionAllColumns,
			monsterKillCountMissionColumnsWithDefault,
			monsterKillCountMissionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			monsterKillCountMissionAllColumns,
			monsterKillCountMissionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert monster_kill_count_missions, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(monsterKillCountMissionPrimaryKeyColumns))
			copy(conflict, monsterKillCountMissionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"monster_kill_count_missions\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(monsterKillCountMissionType, monsterKillCountMissionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(monsterKillCountMissionType, monsterKillCountMissionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert monster_kill_count_missions")
	}

	if !cached {
		monsterKillCountMissionUpsertCacheMut.Lock()
		monsterKillCountMissionUpsertCache[key] = cache
		monsterKillCountMissionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single MonsterKillCountMission record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MonsterKillCountMission) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no MonsterKillCountMission provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), monsterKillCountMissionPrimaryKeyMapping)
	sql := "DELETE FROM \"monster_kill_count_missions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from monster_kill_count_missions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for monster_kill_count_missions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q monsterKillCountMissionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no monsterKillCountMissionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from monster_kill_count_missions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for monster_kill_count_missions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MonsterKillCountMissionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(monsterKillCountMissionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), monsterKillCountMissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"monster_kill_count_missions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, monsterKillCountMissionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from monsterKillCountMission slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for monster_kill_count_missions")
	}

	if len(monsterKillCountMissionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MonsterKillCountMission) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMonsterKillCountMission(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MonsterKillCountMissionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MonsterKillCountMissionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), monsterKillCountMissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"monster_kill_count_missions\".* FROM \"monster_kill_count_missions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, monsterKillCountMissionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in MonsterKillCountMissionSlice")
	}

	*o = slice

	return nil
}

// MonsterKillCountMissionExists checks if the MonsterKillCountMission row exists.
func MonsterKillCountMissionExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"monster_kill_count_missions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if monster_kill_count_missions exists")
	}

	return exists, nil
}

// Exists checks if the MonsterKillCountMission row exists.
func (o *MonsterKillCountMission) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return MonsterKillCountMissionExists(ctx, exec, o.ID)
}
